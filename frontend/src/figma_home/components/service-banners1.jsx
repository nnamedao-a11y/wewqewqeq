/**
 * ServiceBanners1 — formerly rendered the partner-logo strip.
 * The logos have been merged into TurnkeyBanner1 (the consolidated
 * "How to buy a turnkey car" section). This component is intentionally
 * a no-op so the existing Anima homepage stack still imports cleanly.
 */
const ServiceBanners1 = () => null;

export default ServiceBanners1;
