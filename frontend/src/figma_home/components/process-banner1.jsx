/**
 * ProcessBanner1 — formerly rendered the "We send… / We discuss… / …"
 * step list and the PICK UP A CAR / Viber CTAs. All of this content
 * has been folded into TurnkeyBanner1 (the consolidated "How to buy
 * a turnkey car" section). Rendering null keeps the existing Anima
 * homepage import graph intact.
 */
const ProcessBanner1 = () => null;

export default ProcessBanner1;
